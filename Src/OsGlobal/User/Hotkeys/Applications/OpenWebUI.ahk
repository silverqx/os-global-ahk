#Include <OsGlobal\GlobalVariables>
#Include <OsGlobal\Keyboard>

; Open WebUI
; ----------

; Create hotkeys for generating code blocks (at runtime)
CreateAiTextareaHotkeys((*) => WinActive(WinTitleOpenWebUIPwaChrome))
